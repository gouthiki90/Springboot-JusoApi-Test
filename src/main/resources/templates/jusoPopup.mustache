<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <title>Document</title>
</head>

<body>
    <input type="hidden" value="{{roadFullAddr}}" name="" id="roadFullAddr">

    <form action="{{jusoUrl}}" method="get">

        <input type="hidden" value="{{confmKey}}" name="confmKey">
        <input type="hidden" value="{{returnUrl}}" name="returnUrl">
        <input type="hidden" value="{{resultType}}" name="resultType">


        <button id="btn-submit" type="submit">잉</button>
    </form>

    <script>
        let roadFullAddr = $("#roadFullAddr").val();

        if(roadFullAddr == "n") {
            $("#btn-submit").click();
        }else {
            alert(roadFullAddr);
            opener.jusoCallback(roadFullAddr);
            window.close;
        }

    </script>
</body>

</html>